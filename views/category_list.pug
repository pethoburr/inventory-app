extends layout 

block content 
    h1= title
    
    ul 
        each make in make_list 
            li 
                a(href=make.url) #{make.name} #{make.description} 

        else 
            li There are no makes.

